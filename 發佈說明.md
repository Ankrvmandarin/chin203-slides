# 把簡報發佈成學生網站（CHIN 203）

學生用網址線上看簡報，你不用匯出 pptx，也不佔你的硬碟。

---

## 整體流程長什麼樣

```
你的 Mac（localhost）              GitHub 雲端            學生
─────────────────────            ───────────           ──────
跟 Claude 一起做簡報      →   ./publish.sh   →   建置   →   網站更新
草稿隨便改，都是私密的                （只送定稿）
```

**沒有執行 `./publish.sh` 以前，網站上不會有任何變化。** 你在 localhost 怎麼改、改壞了、做到一半，都只在你自己電腦上。

---

## 你要怎麼「挑選」哪幾份上線

用資料夾名字決定，規則只有一條：

**資料夾名稱結尾是 `-draft` 的，只留在你的電腦，不會上線。**

| 資料夾名稱 | localhost 看得到 | 學生網站 |
|---|---|---|
| `l17-yufa-1-draft` | ✅ 可以繼續改 | ❌ 不會出現 |
| `l17-yufa-1` | ✅ | ✅ 會出現 |

所以正常的備課節奏是：

1. 新簡報先叫 `l17-yufa-1-draft`，在 localhost 跟我一起做、反覆改
2. 上完課、確定定稿了，把資料夾**改名拿掉 `-draft`**
3. 下次 `./publish.sh` 它才會上線

執行 `./publish.sh` 時會先**列出這次會上線的清單**，也會列出被留在本機的草稿，你按 Enter 才真的發佈，按 Ctrl+C 可以隨時取消。

---

## 一次性設定（只做這一次，約 10 分鐘）

### 步驟 1 · 在 GitHub 建一個新 repo

到 github.com，右上角 **＋ → New repository**：

- Repository name 填 **`chin203-slides`**（必須完全一樣，設定檔的網址路徑對應這個名字）
- 選 **Public**（GitHub Pages 免費方案需要公開）
- **不要**勾 Add a README
- 按 Create repository

### 步驟 2 · 把工作室連上去並推送

最省事的是用 **GitHub Desktop**（自動處理登入，不用弄 token）：

1. 下載安裝 GitHub Desktop，用你的 GitHub 帳號登入
2. File → Add Local Repository → 選 `~/open-slide-ws/my-slide`
3. 按 **Publish repository**，名稱選 `chin203-slides`，取消勾選 Keep this code private
4. 完成

偏好終端機的話：

```
cd ~/open-slide-ws/my-slide
git remote add origin https://github.com/ankrvmandarin/chin203-slides.git
git add -A
git commit -m "發佈第十六課簡報"
git push -u origin main
```

### 步驟 3 · 打開 GitHub Pages

repo 頁面：**Settings → Pages → Source 選「GitHub Actions」**，存檔。

等 1 到 2 分鐘（repo 上方 **Actions** 分頁看進度），網站就會在：

**https://ankrvmandarin.github.io/chin203-slides/**

把這個網址給學生。

---

## 以後每次更新（一個指令）

```
cd ~/open-slide-ws/my-slide
./publish.sh "更新趋向补语"
```

它會先列出清單讓你確認，再推送。建置在**雲端**進行，你的 Mac 不用跑 build，硬碟不會多出任何檔案，約 1 到 2 分鐘後網站自動更新。

用 GitHub Desktop 的話，就是填一行說明 → Commit → Push origin。

---

## 學生看到什麼

打開網址是**簡報列表**，點任一份就開始看。

- 方向鍵／點擊翻頁
- 你設計的 `<Steps>` 逐步揭示照常運作，學生可以自己一格一格對答案
- 看不到 Presenter 模式、備課筆記、匯出下載

## 你自己上課時不受影響

`pnpm dev` 在本機一律強制打開完整介面，所以 **Presenter 模式、備課筆記、匯出 pptx 全部照舊**。發佈設定只作用在公開網站。

---

## 幾件要知道的事

**筆記的真實狀況**：學生介面看不到備課筆記，但筆記文字仍在網頁程式碼裡，懂技術的人挖得出來。班級情境風險很低，但別在 notes 寫學生個資。

**網站是公開的**：任何拿到網址的人都看得到，這是 GitHub Pages 免費方案的條件。要限制只有選課學生能看，得改用付費方案或放到 Canvas。

**改 repo 名字的話**：`open-slide.config.ts` 裡的 `base` 要跟著改成 `/新名字/`，否則網站會空白。

---

## 檔案位置

- `open-slide.config.ts` · 發佈設定（網址路徑、學生介面）
- `.github/workflows/deploy.yml` · 雲端自動建置
- `publish.sh` · 一鍵發佈（含確認清單）
- `.gitignore` · 草稿規則（`slides/*-draft/`）

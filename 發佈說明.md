# 簡報發佈說明（CHIN 203）

學生用網址線上看簡報，你不用匯出 pptx，也不佔硬碟。

**設定已完成，以下是日常使用方式。**

---

## 現況

| 項目 | 內容 |
|---|---|
| 學生網址 | https://ankrvmandarin.github.io/chin203-slides/ |
| GitHub repo | ankrvmandarin/chin203-slides（public） |
| 你的工作室 | `/Users/ankelu/open-slide-ws/my-slide` |
| 已上線簡報 | l16-d1-kewen、l16-d2-kewen、l16-directional、l16-review、l16-yufa-1、l16-yufa-2 |

---

## 日常流程（你只需要記這個）

### 1 · 做簡報

```
cd /Users/ankelu/open-slide-ws/my-slide
pnpm dev
```

用 **Chrome** 開 `http://localhost:5173`。跟 Claude 一起改，改多久都沒關係，
**這些都在你電腦上，網站不會有任何變化。**

### 2 · 決定哪幾份要上線

規則只有一條：**資料夾名字結尾是 `-draft` 的不會上線。**

| 資料夾名稱 | localhost | 學生網站 |
|---|---|---|
| `l17-yufa-1-draft` | 看得到，可以一直改 | 不會出現 |
| `l17-yufa-1` | 看得到 | 會出現 |

新簡報一律先叫 `xxx-draft`，定稿後把資料夾改名拿掉 `-draft`。

### 3 · 發佈

```
cd /Users/ankelu/open-slide-ws/my-slide
./publish.sh "更新趋向补语"
```

它會先列出這次要上線的清單和被留下的草稿，按 Enter 才真的送出。
建置在 GitHub 雲端跑，你的 Mac 不用 build，約 1 到 2 分鐘後網站更新。

用 GitHub Desktop 也可以：填一行 Summary → Commit → Push origin。

---

## GitHub Desktop 使用注意（今天踩過的坑）

**清單裡可能出現兩個同名的 `chin203-slides`。**
GitHub Desktop 是用「遠端 repo 名稱」命名，不是資料夾名稱。
把滑鼠移上去看路徑才分得出來，**認路徑不要認名字**：

- `/Users/ankelu/open-slide-ws/my-slide` ← 這個才是你的工作室
- 其他路徑 ← 是多餘的空 clone，可以移除

**Local Path 欄位不要打 `~`。** GitHub Desktop 不會展開 `~`，會說「不是 Git repository」。
一律按 **Choose…** 用視窗點選，或貼完整路徑 `/Users/ankelu/open-slide-ws/my-slide`。

**不要按「create a repository here instead」。** 那會在錯的地方建一個空 repo。

---

## 學生看到什麼

打開網址是簡報列表，點任一份就開始看。

- 方向鍵／點擊翻頁
- 你設計的 `<Steps>` 逐步揭示照常運作，學生可自己一格一格對答案
- 看不到 Presenter 模式、備課筆記、匯出下載

**你自己上課不受影響**：`pnpm dev` 一律強制打開完整介面，
Presenter 模式、備課筆記、匯出 pptx 全部照舊。發佈設定只作用在公開網站。

---

## 要知道的事

**網站是公開的**：任何拿到網址的人都看得到（GitHub Pages 免費方案的條件）。

**筆記的真實狀況**：學生介面看不到備課筆記，但文字仍在網頁程式碼裡，
懂技術的人挖得出來。別在 notes 寫學生姓名、成績等個資。

**commit 訊息也是公開的**：任何人打開 repo 頁面看得到你的存檔說明列表，
寫「改了什麼」沒問題，別寫私人的事。

**新增簡報自動出現**：`slides/` 底下新增資料夾，推送後就會出現在列表，不用改設定。

---

## 如果哪天要再開一個新課的網站

**正確順序（不要先在 GitHub 建 repo）：**

1. GitHub Desktop → File → Add Local Repository → **Choose…** 選工作室資料夾
2. 填 Summary → Commit
3. 按 **Publish repository**，名稱直接在這裡填，**它會幫你在 GitHub 上建好**
4. 到 repo 的 Settings → Pages → Source 選「GitHub Actions」
5. 把 `open-slide.config.ts` 的 `base` 改成 `/新repo名稱/`

先在 GitHub 網站建 repo、再用 Publish repository，會出現
「name already exists」錯誤，因為 Publish 是要「新建」不是「連上」。

---

## 檔案位置

- `open-slide.config.ts` · 發佈設定（`base` 網址路徑、學生介面選項）
- `.github/workflows/deploy.yml` · 雲端自動建置
- `publish.sh` · 一鍵發佈（含確認清單）
- `.gitignore` · 草稿規則 `slides/*-draft/`、忽略 `_to_delete/`
